#include<iostream>
using namespace std;

int main()
{
    cout<<"This is the program to understand how the for and while works in detailed \n";
    int i=0;
    // for(;;);
    // cout<<"Came out of infinite loop \n"; // the for is struck in infinite loop 
    
    while(1);
    cout<<"The while came out of infinite while \n"; // the for is struck in infinite loop this wont be printed 
    

}